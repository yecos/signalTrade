{
  "buildCommand": "npx prisma generate && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron?cron=daily",
      "schedule": "0 0 * * *"
    }
  ]
}
